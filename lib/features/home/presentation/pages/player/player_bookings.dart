import 'package:flutter/material.dart';

class PlayerBookings extends StatelessWidget {
  const PlayerBookings({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("player bookings"),),
    );
  }
}
