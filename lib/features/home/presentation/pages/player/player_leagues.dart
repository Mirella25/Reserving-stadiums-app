import 'package:flutter/material.dart';

class PlayerLeagues extends StatelessWidget {
  const PlayerLeagues({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("player leagues"),),
    );
  }
}
